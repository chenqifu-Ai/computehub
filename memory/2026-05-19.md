# 2026-05-19 工作总结

## 🚀 zhangtuo-ai 模型扩展 (14:56 - 16:44)

### 背景
- 老大提供了新 API key: `sk-28PRi...` 对应 `https://ai.zhangtuokeji.top:9090/v1`
- 旧 key 只能访问 1 个模型 (`qwen3.6-35b`)，新 key 能访问 20 个模型

### 执行记录
1. **第1步 - 验证**：确认新 key 可用，返回 20 个模型
2. **第2步 - 替换**：按 MCP-STD-001 替换 `zhangtuo-ai` provider 的 API Key
3. **第3步 - reasoning 修正**：`qwen3.6-35b` 设为 `reasoning=True`（验证输出在 reasoning 字段）
4. **第4步 - 模型扩展**：20 个模型全部写入 `zhangtuo-ai` provider
5. **第5步 - 别名映射**：为 20 个模型添加别名（`zhangtuo`, `ds-v4-flash`, `glm-5`, `kimi-k2.6` 等）
6. **第6步 - 标准验证**：链路验证 ✅ + primary 验证 ✅

### 当前模型清单（zhangtuo-ai）
- `deepseek-v3.1:671b` → ds-v3.1:671b
- `deepseek-v4-flash` → ds-v4-flash
- `deepseek-v4-pro` → ds-v4-pro
- `devstral-small-2:24b` → devstral-small-2
- `gemma3:12b` → gemma3:12b
- `gemma4:31b` → gemma4:31b
- `glm-5` → glm-5
- `gpt-oss:120b` → gpt-oss:120b
- `kimi-k2.5` → kimi-k2.5
- `kimi-k2.6` → kimi-k2.6
- `minimax-m2.1` → minimax-m2.1
- `ministral-3:8b` → ministral-3:8b
- `mistral-large-3:675b` → mistral-large-3
- `nemotron-3-nano:30b` → nemotron-3-nano
- `qwen3-coder-next` → qwen3-coder-next
- `qwen3-coder:480b` → qwen3-coder:480b
- `qwen3-next:80b` → qwen3-next
- `qwen3-vl:235b` → qwen3-vl:235b
- `qwen3-vl:235b-instruct` → qwen3-vl:235b-instruct
- `qwen3.6-35b` → zhangtuo

### 备份保留
- `zhangtuo-ai-common/qwen3.6-35b-common` — 保留旧 key 作为备份

### 系统状态
- **本机负载**: load 31→20（下降中）
- **本机内存**: 8.5G/11G + Swap 7.3G（紧张）
- **本地磁盘**: 83% (382G/463G)

---

## 🎬 Gallery v2 全功能改版 —— 部署完成

### 已完成

#### Gallery 界面改版
- **上传区重做**：支持多文件拖拽/点击上传，文件自动识别类型
- **文件识别系统**：PDF→📄文档内容、WAV→🎤语音配音、JPG→🖼图片素材
- **一键生成**：上传文件列表后点击「🎬 生成视频」，自动触发管线
- **进度面板**：实时显示任务进度（3秒轮询），百分比+阶段文字
- **搜索/筛选/排序**：按文件名搜索，按类型筛选，多种排序方式
- **删除确认**：弹窗确认 + Toast 通知

#### 后端新增
- `POST /api/v1/gallery/generate` — 多文件上传+触发视频生成
- `GET /api/v1/gallery/tasks` — 任务进度查询
- `classifyFile()` — 文件类型智能识别 + 角色标注
- Gallery API 返回扩展（含 `tasks` 字段）

#### Git 追溯系统
- ECS `pipeline-repo/` 仓库初始化完成（3个commit）
- 5条历史任务归档
- gallery.go v2 源码已入库

### 待办
- [ ] Gallery 路径问题排查（上次联农视频丢失根因）
- [ ] 多文件上传到管线的完整链路加固
- [ ] Gallery 帮助页面 / 使用说明书
- [ ] 端到端测试

### 技术笔记
- 旧版 gallery.go 不注册 `/` 路由（避免与 gateway.go 冲突）
- `findVideoWorkerScript()` 在 video.go 中已有定义，gallery.go 中重复定义会导致编译错误
- 两次部署问题：第一次漏了 upload-zone 的 onclick，第二次旧进程未完全杀死占着端口
- 垃圾文件 `null`（0字节）在 gallery 目录中导致 JS 渲染异常

---

## 🐛 Gallery v2 紧急修复 (09:50)

### 修复清单
1. **`let uploadedFiles` 重复声明** — JS 直接报错导致整页白屏，所有功能失效
2. **🗑 删除按钮** — `escapeHtml` 导致含特殊字符的文件名 onclick 断掉，改用 `encodeURIComponent`/`decodeURIComponent`
3. **🎬 生成视频按钮** — 从隐藏的 `file-list` 里拎出来，改为**始终可见**（灰色 disabled，上传文件后变亮）
4. **残留进度文件清理** — 上次重启时 Python 管线被杀导致卡住的任务文件已清理

### 当前系统状态 (09:54)
- **ECS服务器**: 36.250.122.43 ✅ 在线
- **ComputeHub进程**: gateway + worker 双进程运行中
- **Worker**: node-id=ecs-p2ph, interval=3s, concurrent=8, heartbeat=10s
- **Gallery**: 31件作品在线（4个视频，其余为图片/文档/二进制）
  - 视频: world_chinese_foundation_v3.mp4 (17MB), audio_fix_test.mp4, test_8page_154850.mp4, api_test_video.mp4
- **端口**: 8282 正常监听
- **视频管线**: `/home/computehub/src/scripts/video_pipeline.py` (533行) — 支持文字动画/图片合成/TTS配音/多场景拼接/字幕叠加
- **视频模块**: `src/scripts/video/` 含 doc_parser.py, tts_engine.py (edge-tts), video_pipeline.py (重构版), video_worker.py
- **OUTPUT_DIR**: 配置为 `~/gallery`，实际目录 `/home/computehub/gallery/`
- **config.json**: gallery.root_dir=~/gallery (需确认是否对齐)
- **待验证**: 上传→生成→播放的完整端到端流程（老大已提出要重走一次流程）
