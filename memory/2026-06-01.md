# 2026-06-01 日记

## ⚠️ 项目焦点修正（老大确认）
**当前正在开发的项目**: OPC → `/data/data/com.termux/files/home/OPC/`
**已停用的项目**: ComputeHub → `/root/.openclaw/workspace/projects/computehub/`（老大已经停了好几天）

**OPC 最新状态**:
- 版本: v1.2.3（最新commit: 31acc57）
- 分支: master
- 最后修改: `findDeployDir` 修复（服务器升级循环bug）
- 结构: 独立 Go 工程，cmd/ src/ deploy/ scripts/
- 部署: deploy/1.2.3/ 下有各平台二进制

**后续所有操作都以 OPC 为主，不再关注 workspace/computehub**

之前系统消息里的 SIGTERM / certutil 等都是旧 computehub 残留，已处理干净。

---
- `computehub` → Gateway（默认）
- `computehub worker --agent` → Worker Agent（带脑子）
- `computehub --help` → 帮助

### 改动
1. `main.go` — 分发器（检测 os.Args[1] 分发）
2. `internal/gatewayrun/run.go` — Gateway 启动逻辑（从 cmd/gateway 提取）
3. `internal/workerrun/` — Worker 全部逻辑（package workerrun，Run() 导出）
4. `cmd/gateway/main.go` + `cmd/worker/main.go` — 保留旧入口但不再是顶层

### 编译
- linux-amd64: 11MB ✅
- linux-arm64: ✅
- windows-amd64: 11MB ✅
- 全平台交叉编译一次性通过

### 部署
- ECS: Gateway + Agent Worker 双进程运行 ✅
- deploy/windows-amd64/ 已更新（含 computehub.exe / computehub-win-amd64.exe）
- deploy/linux-amd64/ 已更新
- 旧 `computehub` binary 备份为 computehub.bak.0.7.6

### 旧 WMI 退役
- Windows-mobile 节点心跳来自 Gateway 自动召回伪节点
- 旧 WMI `.exe.old.wmi` 已删除
- 等待老大推新版 computehub.exe worker --agent

## 系统状态（19:00）
- 本机：负载 0.12, 内存 149MB free / 11G
- ECS：Gateway v0.7.13 ✅, Worker Agent 模式 ✅
- 磁盘：84%

---

## OPC 项目分析记录 (19:15 老大要求重分析)

**路径**: `/data/data/com.termux/files/home/OPC/`
**版本**: v1.2.3 | 分支: master | 最新commit: `31acc57` findDeployDir fix

### 工程概况
- 语言: Go 1.24.0 | 模块: `github.com/computehub/opc`
- 代码: 26,602 行 | 60+ .go 文件 | 17 个内部包
- 依赖: 极轻，仅 `golang.org/x/term` + `x/sys`
- 入口: `cmd/computehub/main.go` (all-in-one主力) / `cmd/gateway/` / `cmd/tui/` / `cmd/node/`

### 17 个包概览
- **scheduler**(2,307行): 队列 + 任务分配 + 断路器 + 抢占
- **kernel**(2,095行): 线性命令队列 + 状态镜像 + NodeManager
- **agent**(3,820行): AI智能体 + Git记忆系统(1,613行) + Tools
- **composer**(1,163行): LLM适配层 + SSE流式
- **workercmd**(2,160行): Worker生命周期 + 内嵌AI Agent(8383端口) + 自动升级
- **gateway**(4,583行): HTTP路由 + Gallery(2,037行文件管理)
- **tuicmd**(2,342行): TUI终端界面
- **visualizer**(2,277行): 数据聚合 + 可视化
- 其他: health/monitor/executor/gene/store/api/discover/prometheus/pure/version

### 今天修复: findDeployDir bug
- Worker自动升级时找到错误的deploy目录（/home/computehub下旧版），导致无限升级循环
- 3个commit修复：向上遍历找.git根目录 + exeDir子目录检查

### 集群状态
- ECS Gateway (36.250.122.43:8282) v1.2.3 ✅
- ecs-p2ph (Linux Worker) ✅ 凌晨离线后自动恢复
- Windows-mobile-01 ✅ 8活跃任务

### 关键教训
- 老大近期只改 OPC 工程，旧 workspace/computehub 已停
- 遗留后台回显 (SIGTERM, certutil) 都是旧 computehub 残留，不影响
