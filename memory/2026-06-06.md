
## 🚨 02:00-02:54 — Windows System32 binary 替换失败事件

**事件**: TUI session (rapid-du) 尝试替换 Windows-mobile 节点的 C:\Windows\System32\computehub.exe

**失败链**:
1. `$env:TEMP\replace.ps1` 路径中 `$` 被 cmd 吃掉 → 文件写到 `C:eplace.ps1` ❌
2. 执行 .ps1 找不到文件 → 卡住 → 49s 超时 → SIGTERM
3. SIGTERM 杀死整个进程树 → Worker 主进程 (computehub.exe) 被杀死 💀
4. Gateway 仍显示 `online`（缓存），但所有 task 卡 pending（共5个）
5. task-87a50d 的 EncodedCommand 代码本身正确（下载→校验→kill→copy→备份→exit 0），但提交时 Worker 已死

**根因**: cmd 吃掉 `$` + Task 超时杀整个进程树 + Gateway 无 zombie 节点检测

**输出文档**:
- `WIN-REPL-001` → 已更新至 v1.1（新增经验2b、检查清单、恢复步骤）
- `memory/topics/技术经验/WindowsSystem32替换失败复盘.md` → 新建

**当前状态**: Windows-mobile zombie (online but dead)，需人工重启

**操作参数**:
```cmd
cd C:\Windows\System32
computehub.exe worker --gw http://36.250.122.43:8282 --node-id Windows-mobile --interval 3 --concurrent 4 --heartbeat 10
```
