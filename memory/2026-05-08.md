## 🐛 Gateway 路由问题发现 (09:30 深度研究)

### 🔴 3 个处理器未实现
注册了路由但函数不存在，访问会返回 500：
- `handleTaskDetail` — `/api/v1/tasks/detail`
- `handleTaskPoll` — `/api/v1/tasks/poll`
- `handleTaskProgress` — `/api/v1/tasks/progress`

### 🟡 路由重复注册
34 条路由注册 → 15 个唯一处理器
重复了两次，说明代码逻辑有冗余

### 🔴 config.json 包含 API Key
`"api_key": "sk-65ca99f6fd55437fba47dc7ba7973242"`
必须移到环境变量或 .env 文件

### 📋 改进优先级
- 🔴 P0: 实现缺失的 3 个处理器
- 🔴 P0: config.json API Key 安全加固
- 🟡 P1: 补充 scheduler/preempt.go 测试
- 🟡 P1: 添加认证/限流/CORS 中间件
- 🟢 P2: 优雅停机、请求日志、API 文档

### 💡 建议
路由注册应抽成数组 + 循环注册，编译时验证处理器是否存在

---

## 🚀 ComputeHub Gateway Ubuntu 部署准备 (09:45)

### ✅ 部署包已完成

**文件位置**: `/root/.openclaw/workspace/projects/computehub/deploy/ubuntu/`
**压缩包**: `computehub-gateway-v0.7.1-ubuntu-amd64.tar.gz` (9.1MB)

**目录结构**:
```
ubuntu/
├── bin/
│   ├── computehub-gateway    (8.4MB, ARM64 静态编译)
│   └── computehub-worker     (8.1MB, ARM64 静态编译)
├── config/
│   └── config.json           (环境变量注入 API Key)
├── data/                     (运行时数据目录)
├── scripts/
│   └── deploy.sh             (一键部署脚本)
├── systemd/
│   └── computehub-gateway.service (systemd 服务)
├── README.md                 (详细部署指南)
└── QUICKSTART.md             (快速部署指南)
```

### 📋 部署步骤

1. **上传到 Ubuntu 服务器**
   ```bash
   scp computehub-gateway-v0.7.1-ubuntu-amd64.tar.gz user@ubuntu-server:/tmp/
   ```

2. **SSH 连接并执行部署**
   ```bash
   ssh user@ubuntu-server
   cd /tmp
   tar -xzf computehub-gateway-v0.7.1-ubuntu-amd64.tar.gz
   cd ubuntu
   sudo bash scripts/deploy.sh
   ```

3. **验证部署**
   ```bash
   # 检查服务状态
   sudo systemctl status computehub-gateway
   
   # 测试 API
   curl http://localhost:8282/api/health
   
   # 查看日志
   sudo journalctl -u computehub-gateway -f
   ```

### ⚙️ 配置说明

**环境变量文件**: `/opt/computehub/config/.env`
```bash
COMPUTEHUB_API_KEY="your-api-key-here"
```

**配置文件**: `/opt/computehub/config/config.json`
- 使用 `${COMPUTEHUB_API_KEY}` 引用环境变量
- 端口: 8282
- 最大连接数: 100

### 🛡️ 安全加固

- ✅ API Key 从 config.json 移至 .env 文件
- ✅ systemd 服务使用 `compute` 用户运行
- ✅ 文件权限限制 (600)
- ✅ systemd 安全策略 (NoNewPrivileges, ProtectSystem)

### 📊 系统要求

- **操作系统**: Ubuntu 20.04+ / Debian 11+
- **架构**: ARM64 (当前) / AMD64 (需重新编译)
- **内存**: 2GB+ (推荐 4GB)
- **磁盘**: 1GB+
- **权限**: root 或 sudo

### 🔄 后续工作

1. **编译 AMD64 版本** - 如需在 x86_64 Ubuntu 部署
2. **补充缺失的路由处理器** - handleTaskDetail, handleTaskPoll, handleTaskProgress
3. **添加认证中间件** - 保护 API 端点
4. **配置防火墙** - 只开放必要端口

**部署包已准备就绪！** 🎉

---

## 🚀 ComputeHub 升级路线图 (11:50)

### 📋 已创建文档
- **升级路线图**: `projects/computehub/UPGRADE_ROADMAP.md`
- **部署 README**: `projects/computehub/deploy/ubuntu/README.md`

### 🔴 紧急修复 (v0.7.2 - 1-2 周)
1. 修复 3 个缺失的路由处理器 (handleTaskDetail, handleTaskPoll, handleTaskProgress)
2. 配置 API Key 环境变量，移除硬编码
3. 移除重复路由注册

### 🟡 核心改进 (v0.8.0 - 1-2 月)
4. 测试覆盖率提升 (72% → 85%+)
5. 安全防护加固 (JWT 认证、Rate Limiting、CORS)
6. 系统稳定性提升 (优雅停机、健康检查、内存监控)
7. Ollama 集成支持 (deepseek-v4-flash, qwen3.6-35b)

### 🟢 架构优化 (v0.9.0 - 3-6 月)
8. 微服务拆分 (Gateway/Worker/TUI 独立部署)
9. 存储优化 (PostgreSQL + Redis 持久化)
10. 监控告警系统 (Grafana + 邮件/钉钉告警)
11. 分布式节点发现 (完善广播/多播机制)

### 🔵 高级功能 (v1.0.0+ - 6 月+)
12. 计费与多租户 (B2B 拼团模式)
13. GPU 资源管理 (MIG 分割、分布式调度)
14. 可视化增强 (3D 地图、实时监控面板)
15. 文档完善 (OpenAPI、最佳实践、示例代码)

### 🎯 优先级排序
**第一阶段 (立即)**: 修复路由处理器、配置 API Key 环境变量
**第二阶段 (1-2 周)**: 补充测试、添加认证中间件、优雅停机
**第三阶段 (1-2 月)**: Ollama 集成、节点发现、监控告警
**第四阶段 (3-6 月)**: 微服务拆分、存储持久化、计费多租户

### 💡 配置变更
- **AI 模型**: 从 qwen-turbo 改为 Ollama deepseek-v4-flash
- **API 地址**: 从阿里云 DashScope 改为本地 Ollama (localhost:11434)
- **执行模型**: 从 ["qwen-turbo", "qwen-turbo", "qwen-turbo"] 改为 ["deepseek-v4-flash", "qwen3.6-35b", "llama3.1:8b"]

**升级路线图已记录，可逐步实施。** ✅
