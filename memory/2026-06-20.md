## 🏯 交流大厅 v2.0 — 前端API对接 + 消息持久化 (21:17-21:37)

### 任务背景
老大说先搞定交流大厅。当时现状：
- ECS:8282/ai 已有带标签页切换的漂亮 UI（AI对话 + 交流大厅 + 话题）
- 但交流大厅数据是**硬编码**的 `defaultMsgs`，不走 API
- 后端 `/api/v1/hall/*` 早已实现（gateway_hall.go），有39条真实消息（WS加入公告）
- Portal: `gateway.go` 的 `handleAIPage` 从磁盘 `web/ai.html` 读取

### 前端改动 (web/ai.html)
完全重写大厅 JS，从硬编码改为真实 API 对接：

| 功能 | 实现 |
|------|------|
| `loadHallChat()` | `fetch(/api/v1/hall/messages?topic=general&limit=100)` 全量加载 |
| `startHallPoll()` | `setInterval(3000)` 轮询 `/api/v1/hall/messages?since_seq=N` |
| `sendHallMsg()` | `POST /api/v1/hall/post` → 轮询自动拉新消息 |
| 时间分组 | `getDateKey()` 计算日期，插入分隔线 |
| 自动滚动 | 检测距底部 <60px 时自动 scroll |
| 话题标签页 | `refreshTopics()` → `/api/v1/hall/topics` → 点击切换话题 |
| 智能emoji | 根据 from 字段自动匹配头像样式（dz/xz/system/ecs/phone） |

### 后端改动 (gateway_hall.go + gateway.go)
**新增持久化**（之前消息只存内存，重启全丢）：

- `InitHallData(binaryDir)` — 解析 `hall_data.json` 路径（与 binary 同目录）
- `saveHallData()` — goroutine 异步写入（每次 PostHallMessage 后触发）
- `loadHallData()` — Gateway 启动时自动加载
- 结构: `HallData{Messages, Topics, NextSeq}`
- 限制: 保留最近 500 条

**gateway.go**: `ServeWithServer()` 中调用 `InitHallData(exeDir)` 启动加载

### 部署
- 交叉编译: `GOOS=linux GOARCH=amd64 CGO_ENABLED=0` → 14MB binary
- scp → ECS `~/ComputeHub/deploy/computehub.new`
- 旧 Gateway 仍在运行（端口被占），新 binary 启动失败
- **当前状态**: 新版 binary 已上传但未完全生效，需要停旧起新

### 小智的消息
- 跨 session 消息（arm agent 侧）：三国志后端已上线 ECS:8080 ✅，目录 `~/three-kingdoms/` 已建好，后端 Go 编译运行完成，缺数据文件（`generals.json`/`cities.json`）
- 交流大厅实时消息 #57: "收到我的消息吗"（小智在 hall 里问的）
- 老大说先弄大厅再搞三国志

### 待办
- [ ] 杀旧 Gateway 进程让新版 binary 生效（持久化才能写入）
- [ ] 把三国志 JSON 数据 scp 给小智
- [ ] 小智回复"收到我的消息吗" — 需要在大厅回应