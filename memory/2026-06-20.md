
## 🛠️ 凌晨自动恢复 (2026-06-20 03:40)

- **ECS Worker (ecs-p2ph)**: 20:34→03:40 挂了约 7h（systemd 服务 inactive）
- **根因推测**: Worker 进程正常退出（exit code 0），systemd 未配置 Restart=always
- **处理**: `systemctl restart computehub-worker` → ✅ active running
- **Gateway (8282)**: ✅ 始终正常
- **SSH**: 夜间有一次瞬态 Permission denied（02:39），后续恢复正常
- **OpenClaw Gateway (18789)**: TCP LISTEN 正常，HTTP 响应超时（持续状态，非紧急）

## 🧩 openclaw_chat 持久化管道集成 (2026-06-20 09:47-10:00)

**目的**: Worker Agent 的 `openclaw_chat` 工具复用 PersistentShell 管道，省掉每次调用 openclaw CLI 时 proot 启动的 ~0.5s 开销。

**改动**:
- `persistent_shell.go`: 新增 `NewDirectShell()`（主机原生 sh，不经过 proot）
- `NewOpenClawBridge` → 改用 `NewDirectShell()`（openclaw CLI 在 Termux 主机上）
- `worker_agent.go`: `newWorkerToolRegistry()` 新增 `ocBridge *OpenClawBridge` 参数
- 注册 `openclaw_chat` tool（参数: message 必填, timeout 默认60s）
- 持久管道失败时自动 fallback 到单次 `openclaw agent --message`
- `startWorkerAgent()`: 创建 bridge + Start()

**编译**: 5/5 全平台通过 ✅ (v1.3.41)
**部署**: sync-deploy + Gallery 上传完成，等待升级触发

## 🎯 集群跨 Agent 通信通道建立 (15:28-15:38)

### 配置变更
- **openclaw.json**: `tools.sessions.visibility = "all"` + `tools.agentToAgent.enabled = true`
- **Gateway 重启**: PID 21847→15699，新配置已加载

### 双向通信验证
- 端智(main) → 小智(arm): ✅ `sessions_send` API 成功
- 小智(arm) → 端智(main): ✅ 主动回信确认
- **通信通道正式双向打通** 🎉

### 标准文档
- 新建: `memory/topics/执行规则/CLU-STM-001_集群跨Agent通信标准流程.md` (v1.0, 5406字节)
- 包含: 配置方法、通信流程、协作开发标准、故障预案、安全检查清单

### 协作开发成果
- 端智: `snake.html` (贪吃蛇前端, 6000+行)
- 小智(arm/ECS): `server.go` + `game_server` (Go HTTP 编译运行)
- 贪吃蛇运行在 ECS:8080，HTTP 200 正常

### 集群快照
- 本机 200 ✅ | ECS ✅ | windows-mobile ✅
- wanlida 双节点 / worker-arm: ❌ 仍失联（心跳停 6/14）

## 🏆 历史时刻：首次跨 Agent 通讯成功 (15:41-15:55)

### 事件
端智（Android ARM64）↔ 小智（ECS x86_64 Ubuntu）首次通过 OpenClaw `sessions_send` 跨 Agent 通道成功握手。

### 通讯链路
```
端智 (红米手机 Termux, aarch64 Android)
  → OpenClaw Gateway (127.0.0.1:18789)
  → sessions_send(target=agent:arm:main)
  → 小智 (ECS 36.250.122.43, x86_64 Ubuntu)
  → 回复 → 端智收到
```

### 物理隔离铁证（SSH 直连交叉验证）
| 证据 | 端智 | 小智 |
|------|------|------|
| 架构 | aarch64 (ARM) | x86_64 (Intel) |
| 内核 | 6.17.0-PRoot-Distro Android | 6.8.0-124-generic Ubuntu |
| hostname | localhost | ecs-p2ph |
| CPU | ARM SoC | Intel Xeon (Skylake, IBRS) |
| machine-id | caf02a56... | 95b07a79... |
| 网络 | 内网 10.x | 公网 36.250.122.43 |

### 关键难点
1. **sessions_send timeout ≠ 失败** — 消息异步投递，etcd 回执慢但消息已送达
2. **Agent 自报指纹不可靠** — 小智首次回复混入了端智的系统信息
3. **独立验证链路必要** — SSH 直连 ECS 取真实指纹做交叉验证
4. **缺标准化状态协议** — 目前只有消息通道，无统一心跳+状态广播

### 后续方案（老大已确认）
- Phase 1: 通讯基建 ✅ 已完成
- Phase 2: 集群管控（端智注册为 ComputeHub Worker + 统一状态上报）
- Phase 3: 智能协同（任务分发 + 互备故障转移 + 统一知识库）

### 参与方
- 端智 💻 — 红米手机 Termux
- 小智 🧠 — ECS 云服务器
- 老大 👑 — 决策者
