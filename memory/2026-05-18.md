# 2026-05-18 工作日志

## 15:38 - 视频生产管线测试（ECS 节点）

### 任务
老大要求用现有两台节点（ecs-p2ph, Xiaomi-table-01）测试视频生产管线。

### 执行过程
1. 阅读 `video_pipeline.py` / `video_worker.py` / `video.go` 源码
2. 检查 ECS 和本地节点的依赖：
   - ECS: ✅ python3.12, ffmpeg 6.1, edge-tts, Pillow 11.3, moviepy 2.2, python-pptx
   - 本地 Termux: ⚠️ 缺 lxml（PPTX解析需要），直接用 ECS
3. 将 `scripts/video/` 全套 5 文件 scp 到 ECS `/home/computehub/scripts/video/`
4. 在 ECS 上生成 8 页测试 PPTX
5. 运行 `video_pipeline.py` 命令行执行
6. 通过 Gateway API `POST /api/v1/video/generate` 提交

### 结果
- ✅ **命令行成功**: 8页PPT → 渲染8页 → 语音8段(yunxi) → 编码8段 → 拼接完成
- ✅ **API 成功**: POST /api/v1/video/generate 异步提交，后台执行完成
- ✅ **Gallery 可见**: 2个视频均已出现在 Gallery（http://36.250.122.43:8282/api/v1/gallery）

### 产出
| 文件名 | 大小 | 时长 | 分辨率 | 格式 |
|--------|------|------|--------|------|
| test_8page_154850.mp4 | 753KB | 81s | 1920×1080 | H.264 + MP3 |
| api_test_video.mp4 | 753KB | 81s | 1920×1080 | H.264 + MP3 |

### 修复
- Pipeline 的 OUTPUT_DIR 从 `/var/computehub/gallery` 改为 `/home/computehub/gallery`（与 Gallery Web 配置统一）
- Video API 路由 `/api/v1/video/generate` ✅ 正常工作

### 待改进
- 画面只有黑底白字，缺少商务模板的视觉效果（templates/ 目录为空）
- Xiaomi-table-01 节点没有用到（任务直接跑在 ECS 本地）
- 需要将 Worker 节点能力注册与视频管线对接

### 更新
- HEARTBEAT.md → 更新系统状态 + 新增视频管线测试结果

## 16:08 - 读取《世界华人基金会简介9.2版》PDF

### 任务
老大发送 `/data/data/com.termux/files/home/downloads/世界华人基金会简介9.2版.pdf`，要求查看内容。

### 执行过程
1. 直接 read PDF 文件（二进制 raw view，无法读取文本）
2. 本地 pdftotext / PyMuPDF 不可用（Termux 环境限制）
3. 将 PDF scp 到 ECS，利用 ECS Python 环境安装 pdfminer.six 提取文本
4. 成功提取全部文字内容

### 提取内容概要
- **组织**: 世界华人基金会
- **核心宗旨**: 凝聚5000万+海外华人，弘扬中华文明，推动交流合作
- **主要成就**: 成立10年，促成数百亿美元华商经济合作，几千家合资/外资企业，数百次全球商贸活动，创办《世界杰出华人奖》
- **未来规划**: 已设北京/福建/浙江/广东办事处（15项目数十亿），计划拓展20省办事处，首期总投资100亿元
- **文档来源**: PDF 转为 PPTX 风格简介文档（WPS创建，2025-08-27版本）

### 后续
老大未指示下一步操作，待响应

## 22:30~22:46 - 招商BP视频生成（手动执行）

### 任务
22:30 老大在 TUI 中执行了手动视频生成，源文件为 `ai_zhaoshang_bp.pptx`（招商商业计划书）。

### 执行过程（自动发现，非直接参与）
1. TUI 端通过 `exec` 运行了 `video_pipeline.py` 管线脚本
2. 22:30:51 — 任务启动（task_id: `video_1779114651`）
3. 22:41 — 渲染到 seg_0011.mp4（24MB），middle.mp4（13.6MB）
4. 22:43 — middle.mp4 增长至 58MB，继续合成 intro/outro/subs
5. 22:46 — 最终输出完成
6. 23:08 老大询问"是哪个文件做的" → SSH 排查确认源文件

### 产出
| 文件名 | 大小 | 源文件 |
|--------|------|--------|
| pipeline_tts_v2.mp4 | 148MB (148,870,790 bytes) | ai_zhaoshang_bp.pptx (46KB) |

### 对比分析
- 之前世界华人基金会14页 → 18MB 视频
- 本次招商BP → 148MB 视频（页数远超14页，内容量大）
- Gallery 显示 0 视频（可能未正确识别 `.mp4` 类型）

### 关键发现
- **ECS SSH 再次失效**: `id_ed25519_computehub` 密钥认证失败（Permission denied），与下午故障一致（可能 iptables/fail2ban DROP 规则又触发了）
- 管线在 TUI 端独立自动执行，未走 ComputeHub API 任务系统
- Gallery 计数为 0 视频（但实际有 pipeline_tts_v2.mp4），需要检查 gallery 索引逻辑

### 问题
- SSH 密钥认证仍然不稳定，需要进一步排查 ECS 端
- Gallery 视频计数为 0 与实际不符
- 手动管线未与 ComputeHub 任务系统集成（无 task_id 追踪、无进度上报）
