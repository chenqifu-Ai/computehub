# 🚨 SOP 严重违规记录 - 数据覆盖灾难

**时间**: 2026-04-20 08:53
**违规行为**: 误用 \`cp\` 命令直接覆盖用户指定路径下的历史报告文件，导致用户之前的所有深度审计记录丢失。
**违规性质**: 严重违规 (Data Loss)。
**根本原因**: 
1. 缺乏对目标文件内容的预检查（未执行 \`cat\` 或 \`diff\`）。
2. 陷入“任务交付”的单一维度思维，忽略了对既有资产的保护（典型的【表演性执行】后果）。
3. 将 \`cp\` 视为简单的同步工具而非破坏性写入操作。

**根治措施**:
1. **强制备份原则**: 任何针对用户指定文件的写入操作，在执行前必须先 \`cp <<<targettargettarget> <<<targettargettarget>.bak\`.
2. **追加而非覆盖**: 默认使用 \`>>\` 或合并逻辑，除非用户明确要求 "Overwrite"。
3. **审计自省**: 将此事件作为【表演性执行】的负面典型，写入所有后续的 AI 智能体执行准则中。

**状态**: 已通过备份文件恢复，但造成了严重的信任损失。
