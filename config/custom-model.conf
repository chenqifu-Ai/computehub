# 自定义模型配置 - gemma-4-31b
# 配置时间: 2026-04-23 08:57

[gemma-4-31b]
# 基础配置
provider = "vllm-custom"
base_url = "http://58.23.129.98:8000/v1"
model = "gemma-4-31b"
api_key = "sk-vmkohy18-34ga;sjd"

# 连接设置
timeout = 60
connect_timeout = 10
read_timeout = 30

# 模型特性
description = "vLLM专用高性能工具调用模型"
context_window = 131072  # 128k tokens
max_tokens = 4096
temperature = 0.1
top_p = 0.9

# 性能参数
recommended_batch_size = 4
max_concurrent_requests = 10

# 工具调用能力
tool_calling = true
tool_parallelism = 4
function_calling = true

# 适用场景
best_for = ["tool_calling", "code_generation", "data_processing", "automation"]

# 监控配置
health_check_endpoint = "/health"
health_check_interval = 300

# 备用设置
fallback_model = "deepseek-v3.1:671b"
fallback_provider = "ollama-cloud"

[authentication]
# 认证方式
auth_type = "bearer_token"
token = "sk-vmkohy18-34ga;sjd"

# 头信息配置
headers = {
    "Authorization": "Bearer sk-vmkohy18-34ga;sjd",
    "Content-Type": "application/json"
}

[deployment]
# 部署信息
host = "58.23.129.98"
port = 8001
protocol = "http"
location = "中国大陆"

# 性能指标
estimated_latency = "50-200ms"
throughput = "100-500 tokens/sec"
availability = "99.9%"

# 使用说明
usage_notes = """
1. 此配置用于vLLM部署的gemma-4-31b模型
2. 专为工具调用和代码生成优化
3. 需要有效的API密钥认证
4. 如果连接失败，会自动回退到默认模型
"""

# 最后更新
last_updated = "2026-04-23"
configured_by = "老大"# ⚠️ 废弃配置 - 指向已停用的 qwen3.6-35b API (58.23.129.98:8000)
# 已迁移至 NewAPI (ai.zhangtuokeji.top:9090)
# 已标记: 2026-04-29
